# Optional. Without a key, OrderWatch runs in DEMO MODE with prepared sample AI outputs.
ANTHROPIC_API_KEY=
# Port for the local AI proxy (Vite proxies /api to it).
API_PORT=8787

# Optional absolute SQLite file path. Defaults to backend/data/orderwatch.sqlite.
# DATABASE_PATH=/absolute/path/orderwatch.sqlite
